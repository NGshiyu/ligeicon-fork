interface:
  display_name: "Ligeicon Fork"
  short_description: "Repo-specific patterns and workflows for ligeicon-fork"
  default_prompt: "Use the ligeicon-fork repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true